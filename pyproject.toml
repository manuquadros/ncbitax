[project]
name = "ncbitax"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Emanuel Quadros", email = "manuquadros@gmail.com" }
]
requires-python = ">=3.6, <4"
dependencies = [
    "pandas>=2.2.3",
    "pyarrow>=19.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
