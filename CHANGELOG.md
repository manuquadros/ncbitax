# Changelog

Every released version of ncbitax, newest first, generated by
`pdm run changelog` from the commit history. Do not edit by hand.

## 0.1.3 — 2026-09-15

### Fixed

- Make the wheel-unsafe checkout root private (`d74148c`)
- Treat a corrupt or truncated index pickle as a cache miss (`01932e1`)

## 0.1.2 — 2026-09-15

### Fixed

- Specify engine='python' to be able to use nullable extension dtypes (`2de2721`)
- Resolve data files outside the installed package (`95bade8`)
- Remove dead code ruff flagged, quiet its query() false positives (`cfe331a`)
- Invalidate name index caches when the code that built them changes (`c76ade1`)
- Expire every derived cache when the dump or its parser changes (`1e568ef`)
- Treat a deleted names parquet as a cache miss (`e48a9b0`)

### Changed

- Remove dead else branch (`974d10d`)
- Remove unused NAMEINDEX_CACHE_PATH (`cdbae54`)
- Remove unnecessary sep parameter from NCBIDump (`9116ec7`)

### Internal

- Add ruff and mypy as dev dependencies (`4fb181b`)
- Derive the version from the git tag (`a9aa175`)
- Generate CHANGELOG.md and the next version with git-cliff (`4d29e59`)
- Add `pdm run release` to cut a tag from git-cliff's version (`8ab1d5e`)

## 0.1.1 — 2026-01-26

### Internal

- Remove deprecated pandas COW option (`bf86941`)
- Add pytest, remove force-include (`cf84f32`)

## 0.1.0 — 2025-10-06

### Added

- Add load_df, generalizing the access to tables in the NCBI dump (`a656d63`)
- Add type information to reduce the memory size of data frames (`d351f36`)
- Add placeholder functions for bacteria taxonomy retrieval (`540686c`)
- Is_bacteria and is_bacterial_strain functions (`d2b0979`)
- Add type_material to valid name classes and implement strain name decomposition (`0228537`)
- Remove_citations (`9eff909`)
- Bacteria_name_index (`fcac06f`)
- Ncbitax.nodes (`e1f97d4`)
- Add genus name index (`78fcbd6`)

### Fixed

- Get the name of tables correctly if the .dmp extension is given (`d2ddc4c`)
- Do not shadow the `table` argument (`7b706cc`)
- Increase CSV field size to account for long rows in the citations table (`1a99564`)
- Escape quotes and quote fields containing the separator (`0615332`)
- Only the right separator needs to be detected for quoting sep (`7f30820`)
- Return None when the strain portion of the name is empty (`361a489`)
- Type_material -> type material (`e34ddc0`)
- Correctly decompose type strain names with a None at the species attr (`4dc89a2`)
- Find a species name even when the strain is unknown (`f82e7da`)
- Only remove citations form bacterial species names (`4adec9f`)
- If we get an exact match at the rank of species, we can return (`a6bc4b9`)
- Return species names that match a genus plus some sp.-prefixed string (`c5ed7b3`)

### Changed

- Add dataclass decorator to DecomposedName class (`e9e7b92`)
- Simplify the building of the list of bacteria (`62e4eca`)
- Remove get_bacteria (`270a657`)
- Add elsewhere case to keep the function well defined (`41aad6b`)
- Decompose_strain_name -> decompose_name (`6c94cbf`)
- Log failed querys on the NCBI Taxonomy (`7ad3ffe`)
- Bacterial_name_index, with arg rank (`4b2a308`)
- Cache the loading of nodes and names (`3885c99`)
- Several optimizations (`a121fed`)
- Close the tarfile cleanly (`54b0fd6`)
- Fix several linting issues (`ccf13d7`)

### Internal

- Update author email and configure project build settings (`d56ddc3`)
- Add test_ncbitax.py (`eb7da50`)
- Add missing exports (`ac82eb8`)
- Clean up imports in __init__.py (`6eee335`)
- Bump python version and include namespace package (`b054684`)
- Add tqdm dep (`40a05c5`)
- Add license information (`64c24d0`)
- Ignore .zst files (`d0bf1be`)

